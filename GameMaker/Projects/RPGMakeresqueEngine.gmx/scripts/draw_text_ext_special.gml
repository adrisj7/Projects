/// draw_text_ext_special(x, y, string, sep, w);
// Draws text ext, but with our own special formatting.

var xa  = argument0,
    ya  = argument1,
    str = argument2,
    sep = argument3,
    w   = argument4;

var result = "";

var length = string_length(str);
for(var i = 0; i < length; i++) {
    
}

draw_text_ext(xa, ya, str, sep, w);
